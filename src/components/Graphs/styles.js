import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  line: {
    borderBottom: "10px solid rgba(75,0,130,0.5)",
  },
  bar: {
    borderBottom: "10px solid rgba(75,0,130, 0.5)",
  },
}));
